<div>
  Title: <%= @course.title %><br>
  Description: <%= @course.description %><br>
  Status: <%= @course.status %><br>
  Type: <%= @course.type_course %><br>
</div>
<br>
<%# binding.pry %>
<%=link_to 'Create new Block of Course', new_course_course_block_path(@course)%>
<br>
<div>
  <% @course.course_blocks.each do |block| %>
    <div>
      <%= block.title%> | <%= link_to 'Show', course_course_block_path(@course, block) %>
      <br>
    </div>
  <%end %>
</div>