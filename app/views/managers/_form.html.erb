<%= form_with(model: @manager, url: [@organization, @manager], local: true) do |f| %>
  <%=f.collection_select :user_id, User.order(:email), :id, :email%>
  <%= f.submit 'Add', class: 'btn btn-success btn-sm' %>
<% end %>